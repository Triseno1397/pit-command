# Server-side only. Read by api/parse.js; never bundled into the client.
# Local dev:  copy to .env.local and run `vercel dev` (plain `vite dev` has no /api route).
# Production: set it in Vercel -> Project -> Settings -> Environment Variables.
ANTHROPIC_API_KEY=sk-ant-...
